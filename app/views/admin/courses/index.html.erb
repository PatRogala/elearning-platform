<div class="min-h-screen w-[95%] mx-auto">
  <h1 class="text-3xl font-bold mt-10 text-center">Welcome, <%= current_user.fullname %></h1>
  <h2 class="text-2xl font-bold mt-10 text-center">Our Courses</h2>
  <div class="w-full lg:w-[60%] mx-auto mt-10">
    <%= render "admin/courses/form", course: @course %>
    <div id="courses">
      <%= render @courses %>
    </div>
  </div>
</div>
