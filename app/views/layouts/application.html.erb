<!DOCTYPE html>
<html>
  <head>
    <title><%= company_name %> - Programming Courses</title>
    <meta charset="utf-8">
    <meta name="description" content="<%= company_name %> is a programming courses platform">
    <meta name="keywords" content="programming, courses, ruby, ruby on rails, bootcamp">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="font-Poppins bg-background-dark dark:bg-background">
    <%= render "shared/header" %>
    <div id="flash">
      <%= render "shared/flash", flash: flash %>
    </div>
    <%= yield %>
    <%= render "shared/footer" %>
  </body>
</html>
